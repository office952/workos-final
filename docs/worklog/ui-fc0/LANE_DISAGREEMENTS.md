# UI-FC0 — lane disagreements so far

```text
ONE_SYNTHESIS_WRITER = YES
AVERAGED_AWAY = NO
```

1. **Utilaje vs Procese:** same `OwnerCatalogView` chrome. Processes information model scores 9/10; page signature 6/10. Treating them as “one good admin catalog” hides that Utilaje still needs a capability/machine identity.

2. **Product System admin vs inspect:** `/components` has the better ROLE→TYPE projection and the worst signature (admin page with buttons removed). Do not merge them into one score.

3. **Figma vs runtime shell:** HF lot + Arch C + IA Read Me still encode top-nav. Runtime is V3 sidebar. HF frames remain accepted *content* truth for several pages, not living chrome.

4. **Resources archetypes:** HF category→item, Arch C `?selected=`, V3 flat workspace. Three designs. Living runtime is the flat Resources admin on main; V3-final Owner accept of that Figma family is not closed.

5. **Stale NEXT fields:** living `NEXT_PROGRAM_PRIORITY = WORKOS_UI_UX_FINAL_CLOSURE_V1` vs leftover `POST_INTEGRATION_RECOMMENDED_PROGRAM = WORKOS_PERFORMANCE_AND_LOGIC_EFFICIENCY_V1` and direction-canon `PRODUCT_DEVELOPMENT`. UI/UX living priority wins. PERF slices already integrated. Do not execute a competing PERF_4.

6. **Commercial spine vs Product System:** Clienți / Hub / Cereri / Atelier have distinct floorplans (8–9/10 signature). Product System admin and Utilaje share generic `OwnerCatalogView`. Do not treat “V3 commercial is strong” as proof that Product System is closed.

7. **Operator money presentation:** Quote inspection and Job detail encode Cost intern / Adaos / Marjă as ordinary facts. Domain `ALT_B_SCOPED` already strips those keys for non-owner `commercial` access. Residual issue is presentation law (Owner money on the operator decision page), not a proven API leak. Do not hide this behind “facts section is complete.”

8. **Two client doors:** `/clients` “Client nou” and `/admin/customers` lifecycle. Same customer object, two mental models. Commercial create is the daily path; admin lifecycle is Owner-only. Risk of divergent UX, not a second customer authority in domain.

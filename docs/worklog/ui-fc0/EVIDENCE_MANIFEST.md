# UI-FC0 — fresh runtime evidence manifest

```text
CLASS = FRESH_LOCAL_SYNTHETIC
HISTORICAL_PACKS = NOT_THIS_MANIFEST
REAL_CLOUD = NO
REAL_CUSTOMERS = NO
REAL_REQUESTS = NO
PLAYWRIGHT_AVAILABLE = YES
PLAYWRIGHT_USED = YES
ORIGIN = http://127.0.0.1:5181
API = http://127.0.0.1:8791
DATA_DIR = .tmp/ui-fc0-audit
```

Machine-readable rows: `docs/worklog/ui-fc0/evidence/manifest.json`.
Capture script: `docs/worklog/ui-fc0/capture-runtime.mjs`.

```text
FRESH_RUNTIME_ROUTES_OPENED = 21
FRESH_1440_COUNT = 27
FRESH_1280_COUNT = 12
FRESH_768_COUNT = 12
FRESH_LIGHT_COUNT = 46
FRESH_DARK_COUNT = 4
KEYBOARD = SMOKE_TAB_ON_CLIENTS
FOCUS = NOT_MEASURED_PIXEL
TARGETS = SOURCE_MIN_44_NOT_REMEASURED
OVERFLOW = 0
CLIPPING = NONE_OBSERVED
LOGIN = NOT_MOUNTED_SINGLE_PLANE
CLIENT_HUB = NO_OBJECT_IN_FIXTURE
QUOTE_OBJECT = NO_OBJECT_IN_FIXTURE
JOB_OBJECT = NO_OBJECT_IN_FIXTURE
EXECUTION = NO_PLAN_IN_FIXTURE
```

Empty commercial registries are honest empty states (Clienți / Cereri / Oferte / Lucrări). Product System, Utilaje, Oameni, Catalog, Configurator, Resurse used the local single-plane seed, not Cloud.

## Counts by viewport (filenames)

1440 includes defaults, dark, keyboard, and Cerere-nouă drawer.

CRITICAL/HIGH extras: Sistem produs, Components, Utilaje, Oameni, Catalog, Configurator, Oferte, Lucrări, Cereri, Resurse, Clienți, Atelier — 1280 + 768 light. Dark: Sistem produs, Clienți, Atelier, Resurse.

## Visual notes from opened shots

- `/admin/product-system` is the generic category/item catalog. First object is a family, not a product construction.
- `/admin/workcenters` uses the same catalog chrome; first object is a zone, not a machine profile.
- `/admin/people` uses registry metric cards + rows (commercial clone).
- `/products/:code` is a schema form stack (Față / Volum sections exist because the schema says so, not because a ROLE blueprint chrome exists).
- No horizontal overflow on opened pages.

## Distinguishing historical packs

Do not treat `docs/worklog/screenshots/v3-nav-*.png` or prior V3 proof folders as this wave’s capture.

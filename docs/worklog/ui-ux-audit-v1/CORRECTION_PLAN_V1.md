# Evidence-pack correction plan V1

```text
artifact_readiness: implementation-ready
execution: knowledge-work
COMMIT: NO
PRODUCT_CODE: NONE
```

Owner GO `WORKOS_UI_UX_EVIDENCE_PACK_CORRECTION_V1` is the contract. This file is the HOW.

## Goal Capsule

Make the existing UI/UX evidence pack honest and private enough for a second independent review. Do not change product behavior.

## Units

### U1. Identity + verify-this baseline
Prove HEAD equals expected origin/main and only worklog evidence is dirty.

### U2. Privacy repair
Inspect flagged PNGs and scan all kept PNGs. Recapture seller/people/skills from isolated synthetic fixture or documented full visual mask. Strip identifiers from filenames, routes, assertions, manifests, report, harness output.

### U3. Source-derived route inventory
Parse NEW `apps/web/src/App.tsx` and OLD `C:\w\psiso\frontend\src\App.tsx` (plus related route tables). Write source-to-manifest reconciliation. Do not claim gaps=0 unless source proves it.

### U4. Rebuild screenshot-manifest.csv
One captured row per kept PNG. Matching SHA. Portable ASCII names/IDs. Remove stale withdrawn rows. Blocked/explained are not captures.

### U5. Harden assertions
Critical assertions throw. Reclassify LoginGate/auth-503, blank Intake standalone, client tabs, Product Studio tabs, execution/ops-graph, ambiguous order detail, skills MISSING rows.

### U6. Targeted IA recaptures
Isolated fixture only: atelier populated, machine-blocked execution, manual startable task, in-progress/completed, eligible/ineligible operator, invalid PIN, one narrow admin catalog. Historical Machine Strict only with provenance.

### U7. Report + archive + verify-this
Correct counts. Distinguish coverage kinds. ZIP without self-checksum. External sidecar after ZIP is final.

# UI20-R4 — Owner character directive

```text
OWNER_DIRECTION = CONFIRMED
TOP_NAVIGATION = YES
GLOBAL_LEFT_SIDEBAR = NO
HIGH_PERSONALITY = REQUIRED
PAGE_SPECIFIC_CHARACTER = REQUIRED
SEMANTIC_DYNAMISM = REQUIRED
```

WorkOS must be memorable, fast, calm, and specific to the work. Do not optimize for generic enterprise polish.

S1-A is the accepted structural base, not final visual identity:

1. Object register first (`CER-1042` / `OFT-221` / `LUC-88`).
2. One work surface. No redundant inspector rail.

R4 does not create S2 / S3 motifs or system H. Personality comes from how each instrument transforms work.

G1 remains the quiet resting state. Energy only when missing, blocked, selected, current, or transforming.

# UI20-R3 — Motion

Semantic only. One shot. No loop. Meaning survives reduced motion.

| Event | Node | Track | Duration |
| --- | --- | --- | --- |
| SELECT / CONTEXT | `35:99` track on `35:101` | OPACITY 0.35 → 1 | 150 ms |
| FREEZE | `35:105` track on `35:107` | OPACITY 0.40 → 1 | 200 ms |
| Atelier → Execuție | `35:111` | prototype dissolve | 200 ms |

Prototype dissolves use EASE_OUT, 180–200 ms.

Nothing important depends on animation. Frozen Ofertă is a real structural state, not only a fade.

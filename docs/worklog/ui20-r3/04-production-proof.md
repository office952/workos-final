# UI20-R3 — Production proof

## Atelier — dispecerizare

Node `29:84`, 768 `32:172`.

Answers: ce poate porni, ce este activ, ce este blocat, de ce, ce urmează.

Synthetic operations only: Debitare CNC față, Cant aluminiu, Montare LED. Not new canonical contracts. No named machine identity.

Normal rows stay quiet. Blocked Montare LED is locally salient. Selected detail explains eligibility, not a KPI wall.

No Kanban default. No gauges. No fake utilization.

Scan model: one worklist, state in the row, cause under the blocked row.

First 3 seconds: Atelier / three operations / one local block / open LUC-91 or open the active station.

## Execuție — stație activă

Nodes `30:86` light, `35:2` dark.

Radically different from Cerere/Ofertă: reduced chrome, operation title dominates, compact lineage, history compressed.

Same work object as Flow B: LUC-88 / Cant aluminiu / CER-1042 reachable but secondary.

No fake telemetry. No HMI cosplay.

Light title contrast was corrected after first export (near-white ink bug). Current title uses dark ink.

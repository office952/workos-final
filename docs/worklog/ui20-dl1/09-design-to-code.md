# UI20-DL1 — Design-to-code contract

No React this wave. Runtime remains V3 sidebar until a later implementation GO.

## DESIGN_TO_CODE_CONTRACT

| Concern | Owner |
| --- | --- |
| Type, color, space, focus ring, 44px | CSS tokens / variables |
| Compact shell vs Destinations | CSS + route layout, not business truth |
| ObjectRegister projection | React presentational; IDs from backend |
| Selected part, missing fields, freeze | Backend truth + pending |
| Quote freeze, acceptance, Order | Server confirm only |
| Prototype Destinations clicks | Prototype-only |
| Figma dissolve hop | Prototype; runtime waits for confirm |

## CSS_VS_REACT_VS_BACKEND

CSS: paper, ink, type roles, gutters, column tracks, reduced-motion instant swap of already-true states.

React state: pending, focus, Meniu open, selected part *as projection of server/config state*.

Backend: object IDs, materials, rates, freeze, readiness, lineage.

## PROTOTYPE_ONLY_STATE

Connected page-80 journey, Command overlay demo, MOTION timelines that loop for playback.

## VIEW_TRANSITIONS_CANDIDATES

SELECT, RESOLVE, ENTER_WORK, ADVANCE — same-document morphs after confirm.

## MOTION_FOR_REACT_CANDIDATES

FREEZE dissolve if View Transitions cannot express whole-artifact dissolve. Still one-shot. Never the source of truth.

## MUST_WAIT_FOR_SERVER

Îngheață. Confirmă. Pornește. Încheie operația. Any mutation.

## TOP_NAV_VS_V3_SIDEBAR

IA3 top nav is accepted as UI20 *leading* IA, not Owner-accepted final IA. Implemented runtime stays V3 sidebar. Do not implement the swap in this wave.

## WHAT_MUST_WAIT_FOR_LATER_GO

React, Master Polish, library publish, final visual / IA Owner accept.
